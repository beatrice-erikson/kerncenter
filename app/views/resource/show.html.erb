<br />
<%= @usage_types %>
<br />
<br />
<%= @usage %>
<br />
<%= select_tag 'time',
	options_for_select([
		["Today", DateTime.now.to_date.to_s + "/" + DateTime.now.to_s],
		["Last week", 7.days.ago.to_s + "/" + DateTime.now.to_s]]),
	:onchange => "window.location.replace('/resource/#{@resource.resource}/'+this.value);",
	:prompt => "Select date range" %>

<h1>Resource#show</h1>
<%= @resource.resource.capitalize %>
line_chart(*@resource_vars)

<br />

<!== clickable chart==>
bar_chart(*@resource_vars)